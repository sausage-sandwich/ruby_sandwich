.row
  .col-md-12
    h3.text-capitalize
      = recipe.title

.row
  .col-md-6
    img src=(recipe.image_url(:large) || asset_path('recipe_placeholder.jpg')) class='w-100'
  .col-md-6
    = render partial: 'shared/nutrition_facts', locals: { recipe: recipe }
.row
  .col-md-6
    h5 Ingredients
    table.table.table-sm.table-striped
      - recipe.recipe_ingredients.each do |recipe_ingredient|
        tr
          td.text-right.text-capitalize
            = recipe_ingredient.ingredient.title
          td
            = display_quantity(recipe_ingredient.quantity, I18n.t(recipe_ingredient.unit, scope: 'units', count: recipe_ingredient.quantity))

  .col-md-6
    h5 Instructions
    = text_with_newlines(recipe.body)
